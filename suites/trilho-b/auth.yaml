id: trilho-b-auth
steps:
  - loop:
      times: 1
      case: cases/ingest/ingest-N-auth.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-basic-malformed.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-bearer-empty.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-tampered-jwt.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-webhook-no-hmac.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-webhook-bad-hmac.yaml
  - loop:
      times: 1
      case: cases/trilho-b/auth-refresh-replay.yaml
