id: trilho-b-math
steps:
  - loop:
      times: 1
      case: cases/metering/metering-N-rule-AMOUNT_ZERO.yaml
  - loop:
      times: 1
      case: cases/trilho-b/math-negative-amount.yaml
  - loop:
      times: 1
      case: cases/trilho-b/math-overflow-amount.yaml
  - loop:
      times: 1
      case: cases/trilho-b/math-micro-debit.yaml
  - loop:
      times: 1
      case: cases/auth-register/register-N-rule-INVALID_CPF.yaml
  - loop:
      times: 1
      case: cases/tenants-activate/tenants-activate-N-rule-CPF_FORBIDDEN.yaml
  - loop:
      times: 1
      case: cases/metering/metering-N-rule-INSUFFICIENT_BALANCE.yaml
