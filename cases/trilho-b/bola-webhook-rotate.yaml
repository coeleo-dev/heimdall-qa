id: bola-webhook-rotate
contract: contracts/platform-webhooks-rotate.yaml
kind: S-bola-other
gate: auto
tags:
  - security
diff: {}
path_values:
  webhook_id: "{{other_webhook_id}}"
headers:
  Authorization: Bearer {{jwt}}
expect:
  status: 404
