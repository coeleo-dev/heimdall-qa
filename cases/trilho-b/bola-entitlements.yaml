id: bola-entitlements
contract: contracts/api-entitlements-check.yaml
kind: S-bola-other
gate: auto
tags:
  - security
diff:
  set:
    userId: "{{other_nokr_user_id}}"
headers:
  Authorization: Bearer {{api_key}}
expect:
  status: 403
