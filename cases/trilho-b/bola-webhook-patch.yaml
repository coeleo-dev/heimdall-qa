id: bola-webhook-patch
contract: contracts/platform-webhooks-patch.yaml
kind: S-bola-other
gate: auto
tags:
  - security
diff:
  set:
    description: "Cross tenant update attempt"
path_values:
  webhook_id: "{{other_webhook_id}}"
headers:
  Authorization: Bearer {{jwt}}
expect:
  status: 404
