id: ingest-N-omit-transaction_id
contract: contracts/api-ingest-post.yaml
kind: N-omit-transaction_id
gate: auto
tags:
  - function
diff:
  omit:
    - transaction_id
expect:
  status: 400
generate:
  timestamp: now_iso
