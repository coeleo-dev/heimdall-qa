id: entitlements-check-N-rule-SNAKE_CASE
contract: contracts/api-entitlements-check.yaml
kind: N-rule-SNAKE_CASE
gate: auto
tags:
- function
diff:
  omit:
  - userId
  - featureKey
  set:
    user_id: '{{nokr_user_id}}'
    feature_key: '{{feature_key}}'
expect:
  status: 400
waive:
- p_gap: P-GAP-3
  pack: business.rule
