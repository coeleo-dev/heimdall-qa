id: login-N-rule-INVALID_CREDENTIALS
contract: contracts/auth-login.yaml
kind: N-rule-INVALID_CREDENTIALS
gate: auto
tags:
- function
diff:
  set:
    password: wrong-password
expect:
  status: 401
generate:
  email: secret.email
