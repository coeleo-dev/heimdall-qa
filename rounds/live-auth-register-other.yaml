id: live-auth-register-other
suite: suites/auth-register-other.yaml
mode: review
dimensions:
- function
environment: production
include:
- cases/auth-register-other/register-other-H01.yaml
notes: >-
  Thin helper that creates a second tenant so OTHER_TENANT uses a real foreign
  userId. Does not replay the full register matrix. Capture only other_api_key.
